# Prompt Log

## Задание №1: Определение агентов и их ролей

### Промпт 1
**Инструмент:** Codex  
**Промпт:** "Подготовь описание мультиагентной системы управления гостиницей. Нужно выделить типы агентов: заселение/выселение, уборка комнат, обработка запросов гостей, биллинг оплаты и управление отменами. Для каждого агента опиши роль, входные данные, выходные данные и бизнес-правила. Оформи результат как часть технической документации проекта."  
**Результат:** Подготовлена таблица агентов в `README.md`: указаны роли, входные данные, выходные данные и бизнес-правила для 5 типов агентов гостиничной системы.

### Итого
- Количество промптов: 1
- Что пришлось исправлять вручную: ничего
- Время: ~10 минут

## Задание №2: Разработка прототипа агента на Go

### Промпт 1
**Инструмент:** Codex  
**Промпт:** "Реализуй на Go агента `checkin_checkout` для системы управления гостиницей. Агент должен подписываться на очередь NATS `hotel.tasks.checkin_checkout`, принимать JSON-задачи с данными заселения или выселения, выполнять простую бизнес-логику и публиковать результат в reply subject. Для заселения нужно проверять оплату и готовность номера, для выселения переводить номер в статус `needs_cleaning`."  
**Результат:** Создан Go-агент в каталоге `agent-go`. Реализованы структуры `Task` и `Result`, обработка действий `checkin` и `checkout`, проверка обязательных полей, публикация результата в NATS.

### Итого
- Количество промптов: 1
- Что пришлось исправлять вручную: добавить корректный вывод логов одновременно в консоль и файл через `io.MultiWriter`
- Время: ~25 минут

## Задание №3: Разработка оркестратора на Python

### Промпт 1
**Инструмент:** Codex  
**Промпт:** "Создай Python-оркестратор для гостиничной мультиагентной системы с использованием `asyncio` и `nats-py`. Оркестратор должен подключаться к NATS, отправлять задачу агенту, создавать уникальный reply subject, ожидать результат и возвращать структурированный JSON-ответ."  
**Результат:** Реализован `HotelOrchestrator` в `orchestrator/app/orchestrator.py`. Оркестратор умеет отправлять задачи по subject-ам, подписываться на reply subject, ждать ответ и возвращать результат.

### Итого
- Количество промптов: 1
- Что пришлось исправлять вручную: сделать импорт `nats` ленивым внутри `connect()`, чтобы юнит-тесты с моками запускались без установленного `nats-py`
- Время: ~20 минут

## Задание №4: Настройка коммуникации через NATS

### Промпт 1
**Инструмент:** Codex  
**Промпт:** "Подготовь `docker-compose.yml` для проекта гостиничной системы. Нужно развернуть NATS в Docker, добавить сервис Go-агента заселения/выселения и сервис Python API/оркестратора. Настрой переменные окружения, зависимости сервисов и возможность запуска нескольких экземпляров агента."  
**Результат:** Создан `docker-compose.yml`, Dockerfile для Go-агента и Dockerfile для Python API. Настроены сервисы `nats`, `checkin-agent` и `api`, проброшены порты NATS и FastAPI.

### Итого
- Количество промптов: 1
- Что пришлось исправлять вручную: изменить Dockerfile агента, чтобы сборка корректно работала до появления `go.sum`
- Время: ~20 минут

## Задание №5: Логирование и мониторинг

### Промпт 1
**Инструмент:** Codex  
**Промпт:** "Добавь логирование в Go-агент и Python-оркестратор. Нужны уровни INFO и ERROR, вывод логов в консоль и файл. Также добавь простую метрику: счетчик обработанных задач у агента и счетчик обработанных задач у оркестратора."  
**Результат:** В Go-агент добавлен логгер и счетчик `processedTasks`. В Python-оркестратор добавлен общий модуль настройки логирования `logging_config.py`, счетчик `processed_tasks` и endpoint `/metrics` в FastAPI.

### Итого
- Количество промптов: 1
- Что пришлось исправлять вручную: уточнить запись Go-логов в файл, так как изначально файл создавался, но логгер писал только в консоль
- Время: ~15 минут

## Задание №6: Обработка ошибок и таймаутов

### Промпт 1
**Инструмент:** Codex  
**Промпт:** "Доработай Python-оркестратор: если агент не ответил, нужно выполнять повторную отправку задачи не более 3 раз. Добавь таймаут ожидания результата, логирование ошибок и отдельное исключение после исчерпания всех попыток."  
**Результат:** В `HotelOrchestrator.run_task()` реализованы `max_retries`, `timeout_seconds`, повторная отправка, обработка `asyncio.TimeoutError` и исключение `TaskTimeoutError`.

### Итого
- Количество промптов: 1
- Что пришлось исправлять вручную: ничего
- Время: ~10 минут

## Задание №7: Запуск нескольких агентов одного типа

### Промпт 1
**Инструмент:** Codex  
**Промпт:** "Настрой Go-агент так, чтобы можно было запускать 2-3 экземпляра одного типа и распределять между ними задачи через NATS queue group. Добавь в документацию команду для запуска нескольких экземпляров и объясни механизм балансировки."  
**Результат:** Go-агент подписывается через `QueueSubscribe` на группу `hotel_agents`. В `README.md` добавлена команда `docker compose up --scale checkin-agent=3` и пояснение, что NATS доставляет каждую задачу одному агенту из группы.

### Итого
- Количество промптов: 1
- Что пришлось исправлять вручную: ничего
- Время: ~10 минут

## Задание №8: Создание API для запуска задач

### Промпт 1
**Инструмент:** Codex  
**Промпт:** "Разработай простой REST API на FastAPI для запуска задач гостиничной системы. API должен принимать HTTP-запрос на заселение или выселение, валидировать данные через Pydantic, передавать задачу оркестратору и возвращать ответ агента клиенту."  
**Результат:** Создано FastAPI-приложение в `orchestrator/app/api.py`. Реализован endpoint `POST /tasks/checkin-checkout`, модель `CheckinCheckoutRequest`, обработка ошибок timeout и endpoint `/metrics`.

### Итого
- Количество промптов: 1
- Что пришлось исправлять вручную: ничего
- Время: ~15 минут

## Задание №9: Тестирование системы

### Промпт 1
**Инструмент:** Codex  
**Промпт:** "Напиши модульные тесты для Go-агента стандартным пакетом `testing` и тесты для Python-оркестратора через `pytest` с моками NATS. Нужно проверить успешную обработку задачи, ошибочные условия и retry при таймауте."  
**Результат:** Добавлены Go-тесты для логики заселения, отказа при неготовом номере и выселения. Добавлены Python-тесты для успешного ответа и трех retry при timeout.

### Итого
- Количество промптов: 1
- Что пришлось исправлять вручную: добавить `conftest.py` для корректного импорта пакета `app`; заменить async-тесты на запуск через `asyncio.run()`, чтобы тесты не зависели от `pytest-asyncio`; создать `go.sum` через `go mod tidy`
- Время: ~25 минут

## Задание №10: Документирование архитектуры

### Промпт 1
**Инструмент:** Codex  
**Промпт:** "Создай архитектурную документацию для мультиагентной системы управления гостиницей. Нужно описать компоненты, NATS-взаимодействие, запуск проекта, API-запрос, тестирование и добавить диаграмму взаимодействия в формате Mermaid."  
**Результат:** Создан `README.md` с описанием всех 10 заданий, компонентами системы, командами запуска, примером HTTP-запроса, инструкцией по тестированию и Mermaid sequence diagram.

### Итого
- Количество промптов: 1
- Что пришлось исправлять вручную: ничего
- Время: ~15 минут
